using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Buygoods : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int[] goods = { 10, 80, 120, 200, 280 };
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void buy()
    {
    }
}
